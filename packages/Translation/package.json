{
  "name": "@ooneex/translation",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "deno lint . && deno fmt --check .",
    "lint:fix": "deno fmt .",
    "lint:watch": "deno fmt --watch .",
    "test": "deno test -A --unstable .",
    "test:watch": "deno test --watch -A --unstable ."
  }
}
